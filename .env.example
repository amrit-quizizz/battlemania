# Quiz Generation Mode
# Set to 'mock' to use default questions (no API key needed)
# Set to 'ai' to use AI-generated questions (requires ANTHROPIC_API_KEY)
QUIZ_MODE=mock

# Anthropic (Claude) API Key for AI-powered quiz generation
# Only needed when QUIZ_MODE=ai
# Get your API key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here
